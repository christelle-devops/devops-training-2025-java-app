pipeline {
	agent any

	stages {
		stage('Dummy') {
			steps {
				echo'Jenkinsfile OK - pipeline lancé'
			}
		}
		
		stage('info') {
			steps {
				echo "Job: ${env.JOB_NAME}"
				echo "Build: ${env.BUILD_NUMBER}"
			}
		}
	}
}
